You will also notice that the generated PDF file has a table of contents, each entry mapping to the corresponding page
within the document. In Wrap_Pdf, these entries are logged if a section name is provided to the addPage() method and the
actual table of contents is constructed by the output() method based on these entries.